{
  "name": "json-data-agent",
  "version": "1.0.0",
  "description": "AI chat agent that reads and answers from JSON files",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
